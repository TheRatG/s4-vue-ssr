import {getField} from 'vuex-map-fields';

const getters = {
    getField,
};

const mutations = {};

const actions = {};

const state = () => ({
    rows: [],
});

export default {
    namespaced: true,
    getters,
    state,
    actions,
    mutations,
};